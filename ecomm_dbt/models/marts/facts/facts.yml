version: 2

models:
  - name: fct_sales
    description: "Fact table representing sales transactions"
    columns:
      - name: sale_id
        tests:
          - unique
          - not_null
      - name: total_amount
        tests:
          - not_null
